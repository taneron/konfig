```typescript index.ts
// Get Audit Details Data
const getDataResponse = ukg.auditDetails.getData({
    startDateTime: "STARTDATETIME"
    endDateTime: "ENDDATETIME"
    tableName: "TABLENAME"
    fieldName: "FIELDNAME"
})
```