```typescript index.ts
import { Paylocity } from 'paylocity-typescript-sdk';

const paylocity = new Paylocity({
    // CC JWT Authorization header using the bearer scheme
    bearer: "AUTHORIZATION",
    // Paste JWT here.
    jwtAuthorization: "JWT_AUTHORIZATION",
    clientId: "CLIENT_ID",
    clientSecret: "CLIENT_SECRET"
})
```