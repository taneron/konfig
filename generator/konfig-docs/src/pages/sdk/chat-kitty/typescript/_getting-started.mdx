```typescript index.ts
import { ChatKitty } from 'chat-kitty-typescript-sdk';

const chatKitty = new ChatKitty({
    passwordResetTokenAuthorization: "X_TOKEN",
    clientId: "CLIENT_ID",
    clientSecret: "CLIENT_SECRET"
})
```