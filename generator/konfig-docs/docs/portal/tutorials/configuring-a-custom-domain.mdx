# Configuring a Custom Domain

<Admonition type="note">
Get in touch with dylan@konfigthis.com to ensure your custom domain is configured on Konfig's end.
</Admonition>

## Configuring DNS

Once your custom domain is configured on Konfig's end, you will need to configure your DNS to point to Konfig's servers.

To do this, create a `CNAME` record for your domain and point it to `portal.konfigthis.com`.

### Example

For example if you were to setup a `CNAME` record on
[Cloudflare](https://www.cloudflare.com/) for `docs.[YOUR_DOMAIN]`, it would
look like this:

<Figure caption={<span> Setting up a <b>CNAME</b> DNS Record on <a href="https://www.cloudflare.com/">Cloudflare</a></span> }>
![Cloudflare CNAME Record](/img/custom-domain.png)
</Figure>




