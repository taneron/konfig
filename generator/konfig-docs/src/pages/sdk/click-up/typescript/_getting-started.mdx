```typescript index.ts
import { ClickUp } from 'click-up-typescript-sdk';

const clickUp = new ClickUp({
    /*
     * When testing the ClickUp API in your web browser, you can use a [mock server](https://clickup.com/api) which accepts any value in the **Authorization** header.\
     *  \
     * Or you can use your own ClickUp account by generating a [personal token](https://clickup.com/api).\
     *  \
     * When developing an app for other people to use, you can use an [OAuth flow](https://clickup.com/api).\
     *  \
     * ***Note:** OAuth tokens are not supported when using the **Try It** feature.*
     */
    authorizationToken: "AUTHORIZATION",
})
```