```typescript index.ts
// List assignable groups and agents based on query matched against name
const listMatchingResponse = zendesk.assigneeFieldAssignableGroups.listMatching({
    name: "Johnny Agent"
})
```