```typescript index.ts
import { AppwriteServer } from 'appwrite-server-typescript-sdk';

const appwriteServer = new AppwriteServer({
    // Your project ID
    project: "X_APPWRITE_PROJECT",
    // Your secret API key
    key: "X_APPWRITE_KEY",
    // Your secret JSON Web Token
    jwt: "X_APPWRITE_JWT",
    // 
    locale: "X_APPWRITE_LOCALE",
    // The user session to authenticate with
    session: "X_APPWRITE_SESSION",
    // The user agent string of the client that made the request
    forwardedUserAgent: "X_FORWARDED_USER_AGENT",
})
```