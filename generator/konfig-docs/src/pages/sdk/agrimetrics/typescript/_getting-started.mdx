```typescript index.ts
import { Agrimetrics } from 'agrimetrics-typescript-sdk';

const agrimetrics = new Agrimetrics({
    sec0: "OCP_APIM_SUBSCRIPTION_KEY",
    // For requests to https://app.agrimetrics.co.uk/backend.
    token: "TOKEN",
    // For requests to https://api.agrimetrics.co.uk
    apiKeyHeader: "OCP_APIM_SUBSCRIPTION_KEY",
    // For internal use only.
    xUserHeader: "X_USER_ID",
    // (NOT RECOMMENDED) For requests where sending custom headers is not possible.
    apiKeyQuery: "SUBSCRIPTION_KEY",
})
```