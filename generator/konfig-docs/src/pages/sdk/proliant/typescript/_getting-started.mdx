```typescript index.ts
import { Proliant } from 'proliant-typescript-sdk';

const proliant = new Proliant({
    // Client Credentials Flow
    clientId: "CLIENT_ID",
    clientSecret: "CLIENT_SECRET"
})
```