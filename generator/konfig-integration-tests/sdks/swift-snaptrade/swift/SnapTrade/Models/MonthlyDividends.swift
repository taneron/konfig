//
// MonthlyDividends.swift
//
// Generated by Konfig
// https://konfigthis.com
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct MonthlyDividends: Codable, JSONEncodable, Hashable {

    /** Date used to specify timeframe for a reporting call (in YYYY-MM-DD format) */
    public var date: String?
    public var dividends: [DividendAtDate]?

    public init(date: String? = nil, dividends: [DividendAtDate]? = nil) {
        self.date = date
        self.dividends = dividends
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case date
        case dividends
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(date, forKey: .date)
        try container.encodeIfPresent(dividends, forKey: .dividends)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        date = try container.decodeIfPresent(String.self, forKey: .date)
        dividends = try container.decodeIfPresent([DividendAtDate].self, forKey: .dividends)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("date")
        nonAdditionalPropertyKeys.insert("dividends")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

