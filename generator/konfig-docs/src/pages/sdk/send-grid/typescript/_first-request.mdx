```typescript index.ts
// Retrieve all recent access attempts
const getRecentAccessAttemptsResponse = sendGrid.ipAccessManagement.getRecentAccessAttempts({
    limit: 20
    onBehalfOf: "The subuser's username. This header generates the API call as if the subuser account was making the call."
})
```