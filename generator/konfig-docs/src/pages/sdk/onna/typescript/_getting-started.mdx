```typescript index.ts
import { Onna } from 'onna-typescript-sdk';

const onna = new Onna({
    // The API uses API keys to [authenticate requests](https://dev.onna.com/get-started/authentication).
    clientId: "CLIENT_ID",
    clientSecret: "CLIENT_SECRET"
})
```