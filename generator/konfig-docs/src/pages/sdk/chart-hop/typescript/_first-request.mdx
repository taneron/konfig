```typescript index.ts
// Return the appropriate actions that can be performed on an entity or set of entities
const entityActionsResponse = chartHop.access.entityActions({
    orgId: "ORGID"
    type: "TYPE"
})
```