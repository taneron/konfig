```typescript index.ts
import { Ilert } from 'ilert-typescript-sdk';

const ilert = new Ilert({
    // The Bearer API key of your user <a href='/api-docs/#section/Authentication'>more info</a>.
    apiKey: "AUTHORIZATION"
})
```