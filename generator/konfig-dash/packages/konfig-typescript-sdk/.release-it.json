{
  "git": {
    "tagName": "${npm.name}@${version}",
    "requireCleanWorkingDir": false,
    "push": false,
    "commitMessage": "Release konfig-typescript-sdk@${version}"
  },
  "hooks": {
    "before:release": ["yarn build"]
  }
}
