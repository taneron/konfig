```typescript index.ts
import { Finshark } from 'finshark-typescript-sdk';

const finshark = new Finshark({
    // Please enter into field the word 'Bearer' following by space and JWT
    bearer: "AUTHORIZATION"
})
```