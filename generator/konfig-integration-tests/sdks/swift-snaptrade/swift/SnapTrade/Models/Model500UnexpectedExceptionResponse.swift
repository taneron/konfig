//
// Model500UnexpectedExceptionResponse.swift
//
// Generated by Konfig
// https://konfigthis.com
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Example for a response that failed for unexpected reasons */
public struct Model500UnexpectedExceptionResponse: Codable, JSONEncodable, Hashable {

    public var detail: AnyCodable?
    public var statusCode: AnyCodable?
    public var code: AnyCodable?

    public init(detail: AnyCodable? = nil, statusCode: AnyCodable? = nil, code: AnyCodable? = nil) {
        self.detail = detail
        self.statusCode = statusCode
        self.code = code
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case detail
        case statusCode = "status_code"
        case code
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(detail, forKey: .detail)
        try container.encodeIfPresent(statusCode, forKey: .statusCode)
        try container.encodeIfPresent(code, forKey: .code)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        detail = try container.decodeIfPresent(AnyCodable.self, forKey: .detail)
        statusCode = try container.decodeIfPresent(AnyCodable.self, forKey: .statusCode)
        code = try container.decodeIfPresent(AnyCodable.self, forKey: .code)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("detail")
        nonAdditionalPropertyKeys.insert("status_code")
        nonAdditionalPropertyKeys.insert("code")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

