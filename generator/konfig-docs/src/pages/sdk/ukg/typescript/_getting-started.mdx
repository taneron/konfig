```typescript index.ts
import { Ukg } from 'ukg-typescript-sdk';

const ukg = new Ukg({
    username: "USERNAME",
    password: "PASSWORD",
    clientId: "CLIENT_ID",
    clientSecret: "CLIENT_SECRET",
    redirectUri: "REDIRECT_URI",
    // clientId: "CLIENT_ID"
    // clientSecret: "CLIENT_SECRET"
    // username: "USERNAME"
    // password: "PASSWORD"
    bearer: "TOKEN"
})
```