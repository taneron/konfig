import { TypescriptRelativeNpmLinksClient } from "./index";

describe("typescript-relative-npm-links", () => {
    it("initialize client", async () => {
        const typescriptrelativenpmlinksclient = new TypescriptRelativeNpmLinksClient({
            apiKey: "API_KEY",
        });
    });
});
