```typescript index.ts
// Get historical price candles for an index.
const indicesCandlesRetrieveResponse = marketData.v1.indicesCandlesRetrieve({
    resolution: "RESOLUTION"
    symbol: "SYMBOL"
})
```