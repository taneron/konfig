import { TypescriptArrayQueryStringClient } from "./index";

describe("typescript-array-query-string", () => {
    it("initialize client", async () => {
        const typescriptarrayquerystringclient = new TypescriptArrayQueryStringClient({
            apiKey: "API_KEY",
        });
    });
});
