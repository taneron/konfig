{
  "git": {
    "tagName": "${npm.name}@${version}",
    "push": false,
    "commitMessage": "Release konfig-spectral-ruleset@${version}"
  },
  "hooks": {
    "before:release": ["yarn clean", "yarn build"]
  }
}
