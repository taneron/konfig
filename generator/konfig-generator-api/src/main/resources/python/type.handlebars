# coding: utf-8

{{>partial_header}}

from datetime import datetime, date
import typing
from enum import Enum
from typing_extensions import TypedDict, Literal, TYPE_CHECKING
{{#each models}}
{{#with model}}

{{#each typeCodegenImports}}
{{#if isCircular}}
if TYPE_CHECKING:
    {{importString}}
{{else}}
{{importString}}
{{/if}}
{{/each}}

{{> type_templates/type}}
{{/with}}
{{/each}}