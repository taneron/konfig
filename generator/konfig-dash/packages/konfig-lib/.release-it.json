{
  "git": {
    "tagName": "${npm.name}@${version}",
    "push": false,
    "commitMessage": "Release konfig-lib@${version}"
  },
  "hooks": {
    "before:release": ["yarn test", "yarn build"]
  }
}
