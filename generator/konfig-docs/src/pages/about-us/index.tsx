import React from "react";
import Layout from "@theme/Layout";

export default function AboutUs() {
  return (
    <Layout title={`About Us`} description="Generating SDKs for REST APIs">
      Who we are
    </Layout>
  );
}
