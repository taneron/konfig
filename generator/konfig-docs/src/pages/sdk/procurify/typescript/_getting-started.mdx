```typescript index.ts
import { Procurify } from 'procurify-typescript-sdk';

const procurify = new Procurify({
    username: "USERNAME",
    password: "PASSWORD",
    m2MAuthentication: "M_2_M_AUTHENTICATION",
    clientId: "CLIENT_ID",
    clientSecret: "CLIENT_SECRET",
    redirectUri: "REDIRECT_URI",
    cookieAuth: "SESSIONID",
    // Token-based authentication with required prefix "Token"
    tokenAuth: "AUTHORIZATION"
})
```