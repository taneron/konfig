```typescript index.ts
import { Gusto } from 'gusto-typescript-sdk';

const gusto = new Gusto({
    // The access token
    authorization: "AUTHORIZATION",
    // Enter your token in the format **Token <API key>**
    apiKeyAuth: "AUTHORIZATION"
})
```