{
  "git": {
    "tagName": "${npm.name}@${version}",
    "requireCleanWorkingDir": false,
    "push": false,
    "commitMessage": "Release konfig-openapi-spec@${version}"
  },
  "hooks": {
    "before:release": ["yarn clean", "yarn build"]
  }
}
