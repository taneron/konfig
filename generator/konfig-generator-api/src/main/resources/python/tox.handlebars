[tox]
envlist = py39

[testenv]
passenv = *
deps=-r requirements.txt
     -r test-requirements.txt

commands=
   {{#unless useNose}}pytest --cov={{{packageName}}}{{/unless}}{{#if useNose}}nosetests{{/if}} {posargs}
