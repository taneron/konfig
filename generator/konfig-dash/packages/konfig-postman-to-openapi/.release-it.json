{
  "git": {
    "tagName": "${npm.name}@${version}",
    "push": false,
    "commitMessage": "Release konfig-postman-to-openapi@${version}"
  },
  "release-it": {
    "hooks": {
      "before:release": ["yarn test"]
    }
  }
}
