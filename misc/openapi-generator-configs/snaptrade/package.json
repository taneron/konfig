{
  "name": "snaptrade-java-sdk-config",
  "version": "1.0.0",
  "description": "Configuration for SnapTrade's Java SDK",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "dev": "tsc --preserveWatchOutput --watch --pretty",
    "build": "tsc",
    "preprocess": "node dist/scripts/preprocess.js",
    "generate": "yarn preprocess && sh generate.sh"
  },
  "devDependencies": {
    "@types/node": "^18.11.9",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "yaml": "^2.1.3"
  }
}
