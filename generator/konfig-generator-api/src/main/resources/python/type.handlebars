# coding: utf-8

{{>partial_header}}

from datetime import datetime, date
import typing
from typing_extensions import TypedDict

{{#each models}}
{{#with model}}

class Required{{name}}(TypedDict):
    {{#if hasRequired}}
    {{#each requiredVars}}
        {{#if description}}
    """{{{description}}}"""
        {{/if}}
    {{name}}: {{dataType}}
    {{/each}}
    {{else}}
    pass
    {{/if}}

class Optional{{name}}(TypedDict, total=False):
    {{#if hasOptional}}
    {{#each optionalVars}}
        {{#if description}}
    """{{{description}}}"""
        {{/if}}
    {{name}}: {{dataType}}
    {{/each}}
    {{else}}
    pass
    {{/if}}

class {{name}}(Required{{name}}, Optional{{name}}):
    pass
{{/with}}
{{/each}}