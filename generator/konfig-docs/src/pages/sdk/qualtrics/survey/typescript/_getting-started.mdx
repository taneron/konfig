```typescript index.ts
import { Qualtrics } from 'qualtrics-survey-typescript-sdk';

const qualtrics = new Qualtrics({
    // This is a schema for x-api-token header authentication.
    apiToken: "X_API_TOKEN",
    // Public oauth2 schema.
    clientId: "CLIENT_ID",
    clientSecret: "CLIENT_SECRET",
})