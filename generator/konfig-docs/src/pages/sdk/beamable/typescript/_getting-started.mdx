```typescript index.ts
import { Beamable } from 'beamable-typescript-sdk';

const beamable = new Beamable({
    // Gamer Tag of the player.
    userRequired: "X_DE_GAMERTAG",
    // Customer and project scope. This should contain the '<customer-id>.<project-id>'.
    scope: "X_DE_SCOPE",
    // Signed Request authentication using project secret key.
    api: "X_DE_SIGNATURE",
    // Bearer authentication with an player access token in the Authorization header.
    user: "USER"
})
```