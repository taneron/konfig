{
  "providerName": "walmart.com",
  "openApiRaw": "https://developer.walmart.com/api/detail",
  "securitySchemes": {},
  "homepage": "walmart.com",
  "serviceName": "inventory",
  "apiVersion": "1.0.0",
  "methods": [
    {
      "url": "/v3/feeds",
      "method": "updateBulkInventory",
      "httpMethod": "post",
      "tag": "Inventory",
      "description": "Bulk Item Inventory Update",
      "parameters": [
        {
          "name": "feedType",
          "schema": "string",
          "required": true,
          "description": "The feed Type"
        },
        {
          "name": "shipNode",
          "schema": "string",
          "required": false,
          "description": "The shipNode for which the inventory is to be updated. Not required in case of Multi Node Inventory Update Feed (feedType=MP_INVENTORY)"
        },
        {
          "name": "WM_SEC.ACCESS_TOKEN",
          "schema": "string",
          "required": true,
          "description": "The access token retrieved in the Token API call"
        },
        {
          "name": "WM_CONSUMER.CHANNEL.TYPE",
          "schema": "string",
          "required": false,
          "description": "A unique ID to track the consumer request by channel. Use the Consumer Channel Type received during onboarding"
        },
        {
          "name": "WM_QOS.CORRELATION_ID",
          "schema": "string",
          "required": true,
          "description": "A unique ID which identifies each API call and used to track and debug issues; use a random generated GUID for this ID"
        },
        {
          "name": "WM_SVC.NAME",
          "schema": "string",
          "required": true,
          "description": "Walmart Service Name"
        },
        {
          "name": "file",
          "schema": "string",
          "required": true,
          "description": ""
        }
      ],
      "responses": [
        {
          "statusCode": "200",
          "description": ""
        }
      ]
    },
    {
      "url": "/v3/fulfillment/inventory",
      "method": "getWfsInventory",
      "httpMethod": "get",
      "tag": "Inventory",
      "description": "WFS Inventory",
      "parameters": [
        {
          "name": "sku",
          "schema": "string",
          "required": false,
          "description": "An arbitrary alphanumeric unique ID, specified by the seller, which identifies each item. This will be used by the seller in the XSD file to refer to each item. Special characters in the sku needing encoding are: ':', '/', '?', '#', '[', ']', '@', '!', '$', '&', \"'\", '(', ')', '*', '+', ',', ';', '=', ‘ ’ as well as '%' itself if it's a part of sku. Make sure to encode space with %20. Other characters don't need to be encoded."
        },
        {
          "name": "fromModifiedDate",
          "schema": "string",
          "required": false,
          "description": "last inventory modified date - starting range."
        },
        {
          "name": "toModifiedDate",
          "schema": "string",
          "required": false,
          "description": "last inventory modified date - starting range."
        },
        {
          "name": "limit",
          "schema": "string",
          "required": false,
          "description": "Number of Sku to be returned. Cannot be larger than 300."
        },
        {
          "name": "offset",
          "schema": "string",
          "required": false,
          "description": "Offset is the number of records you wish to skip before selecting records."
        },
        {
          "name": "WM_SEC.ACCESS_TOKEN",
          "schema": "string",
          "required": true,
          "description": "The access token retrieved in the Token API call"
        },
        {
          "name": "WM_CONSUMER.CHANNEL.TYPE",
          "schema": "string",
          "required": false,
          "description": "A unique ID to track the consumer request by channel. Use the Consumer Channel Type received during onboarding"
        },
        {
          "name": "WM_QOS.CORRELATION_ID",
          "schema": "string",
          "required": true,
          "description": "A unique ID which identifies each API call and used to track and debug issues; use a random generated GUID for this ID"
        },
        {
          "name": "WM_SVC.NAME",
          "schema": "string",
          "required": true,
          "description": "Walmart Service Name"
        }
      ],
      "responses": [
        {
          "statusCode": "200",
          "description": ""
        }
      ]
    },
    {
      "url": "/v3/inventories",
      "method": "getMultiNodeInventoryForAllSkuAndAllShipNodes",
      "httpMethod": "get",
      "tag": "Inventory",
      "description": "Multiple Item Inventory for All Ship Nodes",
      "parameters": [
        {
          "name": "limit",
          "schema": "string",
          "required": false,
          "description": "The number of items returned. Cannot be more than 50."
        },
        {
          "name": "nextCursor",
          "schema": "string",
          "required": false,
          "description": "String returned from initial API call to indicate pagination. Specify nextCursor value to retrieve the next 50 items."
        },
        {
          "name": "WM_SEC.ACCESS_TOKEN",
          "schema": "string",
          "required": true,
          "description": "The access token retrieved in the Token API call"
        },
        {
          "name": "WM_CONSUMER.CHANNEL.TYPE",
          "schema": "string",
          "required": false,
          "description": "A unique ID to track the consumer request by channel. Use the Consumer Channel Type received during onboarding"
        },
        {
          "name": "WM_QOS.CORRELATION_ID",
          "schema": "string",
          "required": true,
          "description": "A unique ID which identifies each API call and used to track and debug issues; use a random generated GUID for this ID"
        },
        {
          "name": "WM_SVC.NAME",
          "schema": "string",
          "required": true,
          "description": "Walmart Service Name"
        }
      ],
      "responses": [
        {
          "statusCode": "200",
          "description": ""
        }
      ]
    },
    {
      "url": "/v3/inventories/{sku}",
      "method": "getMultiNodeInventoryForSkuAndAllShipnodes",
      "httpMethod": "get",
      "tag": "Inventory",
      "description": "Single Item Inventory by Ship Node",
      "parameters": [
        {
          "name": "sku",
          "schema": "string",
          "required": true,
          "description": "An arbitrary alphanumeric unique ID, specified by the seller, which identifies each item. This will be used by the seller in the XSD file to refer to each item. Special characters in the sku needing encoding are: ':', '/', '?', '#', '[', ']', '@', '!', '$', '&', \"'\", '(', ')', '*', '+', ',', ';', '=', ‘ ’ as well as '%' itself if it's a part of sku. Make sure to encode space with %20. Other characters don't need to be encoded."
        },
        {
          "name": "shipNode",
          "schema": "string",
          "required": false,
          "description": "ShipNode Id of the ship node for which the inventory is requested"
        },
        {
          "name": "WM_SEC.ACCESS_TOKEN",
          "schema": "string",
          "required": true,
          "description": "The access token retrieved in the Token API call"
        },
        {
          "name": "WM_CONSUMER.CHANNEL.TYPE",
          "schema": "string",
          "required": false,
          "description": "A unique ID to track the consumer request by channel. Use the Consumer Channel Type received during onboarding"
        },
        {
          "name": "WM_QOS.CORRELATION_ID",
          "schema": "string",
          "required": true,
          "description": "A unique ID which identifies each API call and used to track and debug issues; use a random generated GUID for this ID"
        },
        {
          "name": "WM_SVC.NAME",
          "schema": "string",
          "required": true,
          "description": "Walmart Service Name"
        }
      ],
      "responses": [
        {
          "statusCode": "200",
          "description": ""
        }
      ]
    },
    {
      "url": "/v3/inventories/{sku}",
      "method": "updateMultiNodeInventory",
      "httpMethod": "put",
      "tag": "Inventory",
      "description": "Update Item Inventory per Ship Node",
      "parameters": [
        {
          "name": "sku",
          "schema": "string",
          "required": true,
          "description": "An arbitrary alphanumeric unique ID, specified by the seller, which identifies each item. This will be used by the seller in the XSD file to refer to each item. Special characters in the sku needing encoding are: ':', '/', '?', '#', '[', ']', '@', '!', '$', '&', \"'\", '(', ')', '*', '+', ',', ';', '=', ‘ ’ as well as '%' itself if it's a part of sku. Make sure to encode space with %20. Other characters don't need to be encoded."
        },
        {
          "name": "WM_SEC.ACCESS_TOKEN",
          "schema": "string",
          "required": true,
          "description": "The access token retrieved in the Token API call"
        },
        {
          "name": "WM_CONSUMER.CHANNEL.TYPE",
          "schema": "string",
          "required": false,
          "description": "A unique ID to track the consumer request by channel. Use the Consumer Channel Type received during onboarding"
        },
        {
          "name": "WM_QOS.CORRELATION_ID",
          "schema": "string",
          "required": true,
          "description": "A unique ID which identifies each API call and used to track and debug issues; use a random generated GUID for this ID"
        },
        {
          "name": "WM_SVC.NAME",
          "schema": "string",
          "required": true,
          "description": "Walmart Service Name"
        },
        {
          "name": "inventories",
          "schema": "object",
          "required": true,
          "description": ""
        }
      ],
      "responses": [
        {
          "statusCode": "200",
          "description": ""
        }
      ]
    },
    {
      "url": "/v3/inventory",
      "method": "getInventory",
      "httpMethod": "get",
      "tag": "Inventory",
      "description": "Inventory",
      "parameters": [
        {
          "name": "sku",
          "schema": "string",
          "required": true,
          "description": "An arbitrary alphanumeric unique ID, specified by the seller, which identifies each item. This will be used by the seller in the XSD file to refer to each item. Special characters in the sku needing encoding are: ':', '/', '?', '#', '[', ']', '@', '!', '$', '&', \"'\", '(', ')', '*', '+', ',', ';', '=', ‘ ’, '{', '}' as well as '%' itself if it's a part of sku. Make sure to encode space with %20. Other characters don't need to be encoded."
        },
        {
          "name": "shipNode",
          "schema": "string",
          "required": false,
          "description": "The shipNode for which the inventory is requested"
        },
        {
          "name": "WM_SEC.ACCESS_TOKEN",
          "schema": "string",
          "required": true,
          "description": "The access token retrieved in the Token API call"
        },
        {
          "name": "WM_CONSUMER.CHANNEL.TYPE",
          "schema": "string",
          "required": false,
          "description": "A unique ID to track the consumer request by channel. Use the Consumer Channel Type received during onboarding"
        },
        {
          "name": "WM_QOS.CORRELATION_ID",
          "schema": "string",
          "required": true,
          "description": "A unique ID which identifies each API call and used to track and debug issues; use a random generated GUID for this ID"
        },
        {
          "name": "WM_SVC.NAME",
          "schema": "string",
          "required": true,
          "description": "Walmart Service Name"
        }
      ],
      "responses": [
        {
          "statusCode": "200",
          "description": ""
        }
      ]
    },
    {
      "url": "/v3/inventory",
      "method": "updateInventoryForAnItem",
      "httpMethod": "put",
      "tag": "Inventory",
      "description": "Update inventory",
      "parameters": [
        {
          "name": "sku",
          "schema": "string",
          "required": true,
          "description": "An arbitrary alphanumeric unique ID, specified by the seller, which identifies each item. This will be used by the seller in the XSD file to refer to each item. Special characters in the sku needing encoding are: ':', '/', '?', '#', '[', ']', '@', '!', '$', '&', \"'\", '(', ')', '*', '+', ',', ';', '=', ‘ ’, '{', '}' as well as '%' itself if it's a part of sku. Make sure to encode space with %20. Other characters don't need to be encoded."
        },
        {
          "name": "shipNode",
          "schema": "string",
          "required": false,
          "description": "The shipNode for which the inventory is to be updated."
        },
        {
          "name": "WM_SEC.ACCESS_TOKEN",
          "schema": "string",
          "required": true,
          "description": "The access token retrieved in the Token API call"
        },
        {
          "name": "WM_CONSUMER.CHANNEL.TYPE",
          "schema": "string",
          "required": false,
          "description": "A unique ID to track the consumer request by channel. Use the Consumer Channel Type received during onboarding"
        },
        {
          "name": "WM_QOS.CORRELATION_ID",
          "schema": "string",
          "required": true,
          "description": "A unique ID which identifies each API call and used to track and debug issues; use a random generated GUID for this ID"
        },
        {
          "name": "WM_SVC.NAME",
          "schema": "string",
          "required": true,
          "description": "Walmart Service Name"
        },
        {
          "name": "quantity",
          "schema": "object",
          "required": true,
          "description": ""
        },
        {
          "name": "sku",
          "schema": "string",
          "required": true,
          "description": ""
        }
      ],
      "responses": [
        {
          "statusCode": "200",
          "description": ""
        }
      ]
    }
  ],
  "apiBaseUrl": "https://marketplace.walmartapis.com",
  "apiDescription": "Maintaining up-to-date inventory for your items on Walmart.com ensures a great experience for your customers and greater sales opportunities for you.",
  "apiTitle": "Inventory Management",
  "endpoints": 5,
  "sdkMethods": 7,
  "schemas": 25,
  "parameters": 48,
  "difficultyScore": 31.5,
  "difficulty": "Easy"
}