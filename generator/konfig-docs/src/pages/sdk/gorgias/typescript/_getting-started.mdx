```typescript index.ts
import { Gorgias } from 'gorgias-typescript-sdk';

const gorgias = new Gorgias({
    // The username will be the email address of your Gorgias account and the password will be your API key.
    username: "USERNAME",
    password: "PASSWORD"
})
```