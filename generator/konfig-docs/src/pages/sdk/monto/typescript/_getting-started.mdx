```typescript index.ts
import { Monto } from 'monto-typescript-sdk';

const monto = new Monto({
    // The word "Basic" followed by a space and your API Key as base64encoded. Contact support@monto.ai to obtain an API key.
    apiKeyAuth: "AUTHORIZATION"
})
```