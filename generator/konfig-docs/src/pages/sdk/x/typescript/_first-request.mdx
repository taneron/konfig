```typescript index.ts
// List Compliance Jobs
const listJobsResponse = x.compliance.listJobs({
    type: "TYPE"
    complianceJobFields: ["created_at","download_expires_at","download_url","id","name","resumable","status","type","upload_expires_at","upload_url"]
})
```