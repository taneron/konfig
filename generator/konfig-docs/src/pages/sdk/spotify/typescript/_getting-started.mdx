```typescript index.ts
import { Spotify } from 'spotify-typescript-sdk';

const spotify = new Spotify({
    // Spotify supports OAuth 2.0 for authenticating all API requests.
    clientId: "CLIENT_ID",
    clientSecret: "CLIENT_SECRET",
    redirectUri: "REDIRECT_URI",
})