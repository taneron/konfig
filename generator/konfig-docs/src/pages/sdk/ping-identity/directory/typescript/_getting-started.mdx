```typescript index.ts
import { PingIdentityDirectory } from 'ping-identity-directory-typescript-sdk';

const pingIdentityDirectory = new PingIdentityDirectory({
    username: "USERNAME",
    password: "PASSWORD"
})
```